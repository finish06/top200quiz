import React from 'react';

const QuestionCount = ({ scoreCard }) => (
	<div className="row">
		<p className="text-center my-1"> Question {scoreCard[1]} </p>
	</div>
);

export default QuestionCount;
